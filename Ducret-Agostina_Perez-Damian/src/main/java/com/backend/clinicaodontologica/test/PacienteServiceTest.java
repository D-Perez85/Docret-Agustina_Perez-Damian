package com.backend.clinicaodontologica.test;
import com.backend.clinicaodontologica.service.impl.PacienteService;
import org.junit.Test;
import java.time.LocalDate;

import static org.junit.Assert.*;

public class PacienteServiceTest {
  /*   private PacienteService pacienteService = new PacienteService(new PacienteDaoH2());


    @Test
    public void deberiaGuardarUnPaciente(){
        Paciente paciente = new Paciente("Carla", "Balenciaga", 30254789, LocalDate.of(2023, 10, 11), new Domicilio("Rivadavia", 1472, "Ballester", "Bs As"));
        Paciente pacienteGuardado = pacienteService.guardarPaciente(paciente);
        assertTrue(pacienteGuardado.getId() != 0);
    }

    @Test
    public void deberiaRetornarUnaListaNoVacia(){
        assertFalse(pacienteService.listarPacientes().isEmpty());
    }

    */

}
